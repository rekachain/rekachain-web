version: "3.9"

services:
  rekachain-web:
    command: >
      sh -c "
        php artisan config:clear &&
        php artisan migrate &&
        php artisan serve --host=0.0.0.0 --port=8000 & 
        npm run dev
      "
  backend-simple-frs:
    build: false
