// import { localizeEnum } from '@/Helpers/localizationHelper';

export enum ProductProblemStatusEnum {
    DRAFT = 'draft',
    FIXED = 'fixed',
    PROGRESS = 'progress',
    CHANGED = 'changed',
}

// export function getLocalizedProductProblemStatus(status: ProductProblemStatusEnum): string {
//     return localizeEnum('ProductProblemStatusEnum', status);
// }